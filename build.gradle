def javaProjects = [ project(':server'), project(':client') ]

configure(javaProjects) {

    apply from: "$rootDir/gradle/javaprojects.gradle"

}

task wrapper(type: Wrapper) {
    gradleVersion = '4.1'
}

